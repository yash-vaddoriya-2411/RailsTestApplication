<!DOCTYPE html>
<html>
<head>
  <title>Rails App</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_importmap_tags %>

  <style>
      /* Sticky Navbar Styling */
      .navbar {
          position: fixed;
          top: 0;
          left: 0;
          width: 100%;
          background-color: #333;
          padding: 10px 0;
          z-index: 1000;
      }

      .navbar a {
          color: white;
          padding: 10px 15px;
          text-decoration: none;
      }

      .navbar a:hover {
          background-color: #555;
      }

      .content {
          margin-top: 50px; /* To avoid content getting hidden under navbar */
          padding: 20px;
      }
  </style>
</head>
<body>
<!-- Sticky Navbar -->
  <div class="navbar">
    <%= link_to "Capture", new_invoice_path, class: "nav-link" %>
    <%= link_to "Checks", checks_path, class: "nav-link" %>
    <%= link_to "Invoices", invoices_path, class: "nav-link" %>
    <%= link_to "Companies", companies_path, class: "nav-link" %>
  </div>

  <!-- Page Content -->
  <div class="content">
    <%= yield %>
  </div>
</body>
</html>
